import Foundation

/// Layout constants that must remain identical across renderer targets.
package enum ModalLayoutMetrics {
    package static let titleMinimumScaleFactor: CGFloat = 0.75
    package static let titleFontScaleStep: CGFloat = 0.05
}
